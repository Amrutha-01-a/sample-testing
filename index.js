/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';
import DisplayOnBoarding from './DisplayOnBoarding';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => DisplayOnBoarding);
